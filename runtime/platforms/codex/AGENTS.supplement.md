# Codex Supplement

This file is reserved for Codex-specific guidance that extends the shared `runtime/HARNESS.md` baseline.

It should capture runtime differences without replacing the shared philosophy.
